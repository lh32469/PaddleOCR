apiVersion: apps/v1
kind: Deployment
metadata:
  name: paddleocr-easement
  labels:
    app: paddleocr-easement
spec:
  replicas: 2
  selector:
    matchLabels:
      app: paddleocr-easement
  template:
    metadata:
      labels:
        app: paddleocr-easement
    spec:
      containers:
        - name: paddleocr-easement
          image: localhost:32000/paddleocr:main
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8000
          resources:
            requests:
              memory: "1Gi"
              cpu: "500m"
            limits:
              memory: "4Gi"
              cpu: "4000m"
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 30
            periodSeconds: 30
            failureThreshold: 3
          # Delay covers model download (~15 MB) + PaddleOCR warm-up on first start.
          # Models are cached in the container after the first pod run.
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 90
            periodSeconds: 10
            failureThreshold: 12
      # Prevent OOM-killed pods from cycling too fast while a neighbour handles load.
      terminationGracePeriodSeconds: 60
